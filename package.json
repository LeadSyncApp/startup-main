{
  "name": "startup-backup",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "build": "cd startup/startup-frontend && npm install && npm run build"
  },
  "description": "LeadSync CRM application with frontend and backend"
}
