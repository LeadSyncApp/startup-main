{
  "buildCommand": "cd startup/startup-frontend && npm install && npm run build",
  "outputDirectory": "startup/startup-frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
