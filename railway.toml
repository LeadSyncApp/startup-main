[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd startup/leadsync-backend && npm start"
buildCommand = "cd startup/leadsync-backend && npm install --include=dev && npm run build"

[env]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"
