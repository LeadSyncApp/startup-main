[build]
builder = "NIXPACKS"
buildCommand = "cd leadsync-backend && npm ci --include=dev && npm run build"

[deploy]
startCommand = "cd leadsync-backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
NODE_ENV = "production"
